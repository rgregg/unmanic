# Migrations v1

DB migrations inherited from Unmanic >= 0.1.0 and carried forward by
Trawlarr. The schema is unchanged by the `unmanic` -> `trawlarr` rename;
only the database *filename* moved, from `unmanic.db` to `trawlarr.db`.
See `devops/migrations.sh`.
